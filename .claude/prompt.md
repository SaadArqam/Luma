Re-apply the updated color tokens from DESIGN-luma.md across the entire app — this is a values-only update to an already-applied design system, no structural changes.

Every element already using {colors.canvas}, {colors.surface-glass}, {colors.surface-solid}, {colors.surface-solid-raised}, {colors.border-hairline}, {colors.border-hairline-strong}, {colors.accent}, {colors.accent-pressed}, {colors.accent-glow}, {colors.accent-border}, {colors.text-primary}, {colors.text-muted}, {colors.success}, {colors.warning}, or {colors.danger} needs to pick up the new hex/rgba values automatically if these are defined as CSS variables or a theme object in one place — confirm that's the case (search for where these were originally defined when DESIGN-luma.md was first applied) rather than finding every hardcoded usage individually.

If any component has the OLD hex values hardcoded instead of referencing shared tokens (a sign the original application wasn't fully systematic), fix those specific spots to reference the shared token instead of a hardcoded value — this prevents the same "have to hunt everywhere" problem next time a token changes.

Confirm after: canvas vs. card contrast is now visibly stronger, terracotta buttons/accents read as more vivid, borders are visible without squinting, and success/warning/danger colors on the Today ring and budget bars are more saturated than before.